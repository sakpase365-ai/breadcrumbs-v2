import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mannaholdings.breadcrumbs',
  appName: 'Breadcrumbs',
  webDir: 'out'
};

export default config;
